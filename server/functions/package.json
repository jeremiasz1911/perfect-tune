{
  "name": "functions",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "deploy": "npm run build && npm run build:functions && firebase deploy",
    "build:functions": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "express": "^4.18.3",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "node-fetch": "^2.7.0",
    "pdfkit": "^0.17.2"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.13",
    "@types/pdfkit": "^0.17.3",
    "typescript": "^5.4.0"
  }
}
